# VivoKsu / NWFlash 工作接力（2026-08-31）

## 1. 冻结说明

- 用户已要求停止开发、清理临时文件并留下接力文档。
- 所有本地子智能体均已中断；三个兄弟任务均已收到停止指令并处于 idle。
- 当前没有 `cargo`、`rustc`、`rustdoc`、`link`、`cl` 或 `lld-link` 构建进程。
- 未部署 Cloudflare，未签名，未安装，未连接真机，未执行 VMProtect GUI。
- 不要回滚或清理下文列出的脏 worktree；它们包含尚未提交的有效 RED/GREEN 工作。

## 2. 当前可信主线

- 写入本接力文件前的外层基线：`codex/vmp-release-completion@3876d4a467fb6083074c7a87770ddac7353cc618`
- 集成工作区：`codex/integration-staging@0ebd4bf62643e58c7f7bfe396a442b6fbec79b3f`
- 集成工作区在冻结时 clean。
- 当前集成 tip：`feat(trace): add sentinel-attested HTTP transport`

`0ebd4bf` 已包含的重要提交（由新到旧）：

```text
0ebd4bf feat(trace): add sentinel-attested HTTP transport
41185a7 build(vmp): enforce sentinel leaf isolation
334958b feat(trace): require credential sentinel receipts
14fdf7c fix(producer): make event sequencing fail closed
964cb9b docs(usage): record durable V1 reporter boundary
6ac5326 feat(usage): retire Tauri reporter onto durable V1 bridge
35e0248 fix(tauri): close refresh and mirror release gates
```

已经集成并通过既有门禁的主要范围：管理后台、用户后台、文件管理、固件输出取证、驱动归档提权边界、进程观察桥、Plan C Wave 1/Wave 2 基础、producer 原子序号/身份语义、V1 reporter 退役桥、第六个 VMP sentinel 源叶、sentinel-attested HTTP transport。

工程估算（不是测试覆盖率，也不是发布声明）：功能完成度约 `90%`，真实发布就绪度约 `68%`。

## 3. 已完成但尚未集成的 clean 提交

### Operation dispatch authority

- Worktree：`.worktrees/operation-dispatch-guard`
- Branch：`codex/operation-dispatch-guard`
- Commit：`a695c71ab115c86d55d72109220a7a88ac440c0c`
- 标题：`fix(application): seal operation dispatch authority`
- 仅修改：
  - `nwflash-application/src/operation_coordinator.rs`
  - `nwflash-application/tests/operation_coordinator.rs`
- 已有证据：focused 30/30、application lib 26/26、doctest 2/2、clippy `-D warnings`、fmt、diff-check。
- 关闭：cancel 撤销 active authority、operation future 返回即撤销、stale/drop authority、reentry violation latch、panic/poison fail-closed。
- 注意：独立 spec/adversarial reviewer 在给最终 verdict 前被停止。主路径初审符合规格；恢复后仍需完成一次独立最终审查，再决定 cherry-pick。

## 4. 必须保留的脏 worktree

### A. Sealed spool facade

- Worktree：`.worktrees/sealed-spool-facade`
- Branch：`codex/sealed-spool-facade`
- HEAD：`35e0248b38b7f9c62aa4088626a8792fec6574e5`
- 当前文件：
  - M `nwflash-infrastructure/src/lib.rs`
  - M `nwflash-infrastructure/src/trace_spool.rs`
  - M `nwflash-infrastructure/src/trace_uploader.rs`
  - M `nwflash-protection/src/trace_redaction.rs`
  - ?? `nwflash-infrastructure/src/trace_facade.rs`
- 已完成到的语义：
  - facade 只接收 `SentinelAttestedTraceUpload`；raw `SealedTraceUpload` 的 metadata view 保持 private。
  - `metadata_view()` 包含非文本 identity/count/context/body hash+len，包含 later-chunk event/run context。
  - claim 改为使用 `RegisteredSealedAttempt` exact attempt id，不再误取同版本第一条 due。
  - claim 精确比较 owner、client version、body identity、item/revision/parent/trace snapshot。
  - ACK 校验 exact owner/version/attempt/items，mixed ACK 走单锁单持久化 CAS。
- 未完成：最终 focused/fmt、checkpoint commit、rebase 到 `0ebd4bf`、rebased 测试与最终审查。
- 仍存在 P0：metadata-only spool 不持久化 attested HTTP body；重启后没有 live capability/body，也没有 orphan sweep + durable loss tombstone。当前只能 fail-closed，不能宣称恢复或丢失闭环。

### B. Producer sentinel-static

- Worktree：`.worktrees/producer-sentinel-static`
- Branch：`codex/producer-sentinel-static`
- HEAD：`0ebd4bf62643e58c7f7bfe396a442b6fbec79b3f`
- 当前文件：M `nwflash-application/src/trace_producer.rs`
- 仅加入了 `compile_fail` RED 文档；没有实现、提交、测试或审查结果。
- 目标：`TraceMetadataSink` / `TraceRunHandle` 只接受 `SentinelAttestedTraceUpload`，禁止 raw `SealedTraceUpload` 直达 sink/HTTP，同时保留 `f1ddf9d` 的 sequence、identity lease 与 batch 原子语义。
- 已裁决：使用 `compile_fail` doctest，不新增 trybuild，不修改 Cargo.lock。

### C. Process trace adapter

- Worktree：`.worktrees/process-trace-adapter`
- Branch：`codex/process-trace-adapter`
- HEAD：`0ebd4bf62643e58c7f7bfe396a442b6fbec79b3f`
- 当前文件：
  - M `nwflash-application/src/lib.rs`
  - ?? `nwflash-application/src/process_trace.rs`
- 已实现并冻结：完整流扫描、binary stdout 排除、`ObservedProcessOutcome` 按值消费、observer/read/termination/cap/high-risk 失败零上传并终态 Failed、pending 同一 upload IDs 精确重试。
- 当前只有两个预期编译 RED：E0053/E0308；producer trait/handle 仍接收 `SealedTraceUpload`，collector 已只生成 `SentinelAttestedTraceUpload`。
- 不要加入 raw shim；应在 producer sentinel-static 完成后继续。

### D. Producer -> spool adapter RED

- Worktree：`.worktrees/planc-producer-spool-adapter`
- Branch：`codex/planc-producer-spool-adapter`
- HEAD：`0ebd4bf62643e58c7f7bfe396a442b6fbec79b3f`
- 当前文件：?? `nwflash-infrastructure/tests/trace_metadata_adapter.rs`
- protection 的重复 metadata-view 草稿已经撤回，避免与 sealed facade 冲突。
- RED 目标：attested-only、batch atomic、401/403 只暂停 owner、426 全局版本门。
- 应先集成 sealed facade 的 rebased SHA，再继续 infrastructure/Tauri adapter。

### E. Refresh spawn race

- Worktree：`.worktrees/refresh-spawn-race-fix`
- Branch：`codex/refresh-spawn-race-fix`
- HEAD：`35e0248b38b7f9c62aa4088626a8792fec6574e5`
- 当前文件：M `commands/device.rs`、`commands/device_identity.rs`、`commands/root_ota.rs`
- 该分支把最终 executor 前检查推进到 Tauri 边界；既有 focused/full 证据曾通过，但严格原子性仍依赖 application dispatch guard。
- 恢复后应先集成并审查 `a695c71`，再把 `with_running_dispatch` 用到真实同步 spawn 点，最后重新验证。

## 5. 仍未完成的发布级任务

1. 完成并审查 `a695c71`，集成 operation dispatch guard。
2. 完成 sealed facade，保留重启 body recovery P0 的诚实边界。
3. 收紧 producer/sink 为 sentinel-attested 静态类型。
4. 完成 process trace adapter 和 producer -> metadata spool adapter。
5. 设计并实现可恢复的 sealed payload vault，或明确实现重启 orphan -> durable loss 的产品语义；metadata-only manifest 不能单独重建 HTTP body。
6. 完成 Tauri production producer -> spool -> HTTP 接线：静态 identity provider，原子 account owner/login generation/build epoch/token 绑定，安全 shutdown deadline。
7. 将 application dispatch authority 用到 device/root OTA 最终 OS spawn 及必要 HTTP dispatch 边界。
8. 在最终集成 tip 上运行分层全量 Rust、桌面前端、Cloudflare、PowerShell/VMP gate。
9. 更新 `PROJECT_PROGRESS.md` 并制作最终 bundle。
10. 真实发布仍需：VMProtect Lite GUI、compiler log/marker review、protected runtime/CRC、Authenticode、NSIS、安装/卸载、登录/heartbeat、真机 smoke。

禁止把源码/gate ready 表述成“已被 VMProtect 真实保护”。

## 6. 推荐恢复顺序

```text
1. 检查所有 worktree status；禁止 reset/clean 上述五个脏 worktree。
2. 独立复审 a695c71；通过后 cherry-pick 到 integration-staging。
3. sealed-spool-facade：focused -> checkpoint -> rebase 0eb/新integration tip -> 再验证/审查。
4. producer-sentinel-static：只改 application producer/tests，使用 facade 的安全 metadata view。
5. process-trace-adapter：消除 E0053/E0308，完成审查和 commit。
6. planc-producer-spool-adapter：集成 facade/producer，再做 infrastructure + Tauri 接线。
7. refresh-spawn-race-fix：接 application guard，闭合最终 spawn TOCTOU。
8. 处理 sealed payload recovery P0，之后才允许宣称 Plan C production closed-loop。
9. 全量 gate、进度文档、最终 bundle、人工 VMP/签名/安装/真机流程。
```

## 7. 清理与磁盘状态

- 已使用精确 `cargo clean` 清理所有已发现的 worktree/external Cargo targets。
- Cargo 输出累计约 `22.9 GiB` 可重建产物被清理。
- 清理了 `1352` 个本项目临时目录，约 `5.82 GiB`。
- 删除 outer 的 `cloudflare/.wrangler/task-2-dry-run`（3 files / 112,184 bytes）与空 `cloudflare/web/.wrangler`。
- 清理后未发现 target、`.wrangler`、test-results、playwright-report 或 vmp-preflight 残留。
- 冻结时可用空间：C 约 `19.49 GiB`，D 约 `23.36 GiB`。
- 保留且不得删除：
  - `C:\Users\mi\Desktop\VivoKsu-quarantine\20260826\nested-repository`
  - `C:\Users\mi\Desktop\VivoKsu-quarantine\20260826\stale-admin-worktree-copy`
  - `C:\Users\mi\Desktop\VivoKsu-quarantine\20260826\cloudflare-user-wrangler`

## 8. 恢复与备份

最新已验证 bundle：

```text
C:\Users\mi\Desktop\VivoKsu-backups\VivoKsu-20260830-pre-final-wiring.bundle
Bytes: 350,871,198
SHA-256: A1793EAB3B9ECC546C48195A9DCE0A40588B2EA6E6A6929A998BC05244166BE7
git bundle verify: complete history / bundle okay
```

该 bundle 覆盖创建时所有已提交 refs（含 integration `0ebd4bf`），不包含本文件第 4 节列出的未提交 worktree 内容。因此恢复前必须同时保留这些 worktree 的磁盘内容。

VMProtect SDK：

```text
C:\Users\mi\Downloads\VMProtect Lite v3.10.4 Build 2668
```

## 9. 停止点检查清单

- [x] 本地子智能体已中断。
- [x] 兄弟任务已停止并 idle。
- [x] 构建进程为 0。
- [x] integration-staging clean at `0ebd4bf`。
- [x] 临时 Cargo/Workerd/Playwright/VMP preflight 产物已清理。
- [x] 脏 worktree 内容保留，未 reset、未 clean、未覆盖。
- [x] 最新预接线 bundle 已验证。
