# 接力文档 — 2026-09-02

## 当前停止点

- 主工作区：`C:\Users\17254\Desktop\存档\TOOL\VivoKsu 工具`
- 主分支：`codex/vmp-release-completion`
- 集成 worktree：`C:\Users\17254\Desktop\存档\TOOL\VivoKsu 工具\.worktrees\integration-staging`
- 集成分支：`codex/integration-staging`
- 集成 tip：`f565fc4 fix(trace): account for unrecoverable restart attempts`
- 前一关键提交：`196a6c2 fix(tauri): close refresh and identity spawn race`
- 集成 worktree 当前 clean；主工作区仅保留历史未跟踪的 `.workbuddy\` 与临时 patch，不能 broad clean。

## 本轮已完成

### Tauri 刷新/身份读取竞态

`196a6c2` 修改了以下文件：

- `C:\Users\17254\Desktop\存档\TOOL\VivoKsu 工具\.worktrees\integration-staging\src\Nwflash.Desktop\src-tauri\crates\nwflash-tauri\src\commands\device.rs`
- `C:\Users\17254\Desktop\存档\TOOL\VivoKsu 工具\.worktrees\integration-staging\src\Nwflash.Desktop\src-tauri\crates\nwflash-tauri\src\commands\device_identity.rs`
- `C:\Users\17254\Desktop\存档\TOOL\VivoKsu 工具\.worktrees\integration-staging\src\Nwflash.Desktop\src-tauri\crates\nwflash-tauri\src\commands\root_ota.rs`

实际 OS `ProcessExecutor::run` 前新增最终 admission 检查，覆盖 discovery、ADB `getprop`、电量读取和 ROOT OTA 身份读取；失败分类不会泄露 serial、命令行或远程 URL。相关 TOCTOU/refresh race 测试已加入。

### metadata-only spool 重启丢失闭环

`f565fc4` 修改了：

- `C:\Users\17254\Desktop\存档\TOOL\VivoKsu 工具\.worktrees\integration-staging\src\Nwflash.Desktop\src-tauri\crates\nwflash-infrastructure\src\trace_spool.rs`
- `C:\Users\17254\Desktop\存档\TOOL\VivoKsu 工具\.worktrees\integration-staging\src\Nwflash.Desktop\src-tauri\crates\nwflash-infrastructure\src\trace_facade.rs`

启动时会对 `Pending`、`Inflight`、`NeedsRemediation`、`Paused` orphan attempt 做 fail-closed 处理：

1. 为相关 trace 写入 durable loss tombstone，reason 为 `restart_payload_unrecoverable`；
2. 从 manifest 移除相关 items/attempts；
3. 后续相同 trace 注册返回 `ExpiredTrace`；
4. tombstone 读取同时接受 `retention_expired_7d` 和 `restart_payload_unrecoverable`。

这不是 payload replay。attested HTTP body 和 `RegisteredSealedAttempt` 仍只存在内存；真正的重启重放必须另行实现受保护 payload vault 或可恢复 attested run-record。当前保证的是“不误恢复、不静默丢失”。

## 验证状态

已执行：

- `git diff --check`：通过。
- 集成 worktree `git status --short --branch`：clean。

未执行：

- `cargo fmt --check`
- `cargo test`
- `cargo clippy -- -D warnings`

原因：当前主机找不到 `cargo`、`rustc`、`rustup`。Rust 工具链恢复后，工作目录应为：

`C:\Users\17254\Desktop\存档\TOOL\VivoKsu 工具\.worktrees\integration-staging\src\Nwflash.Desktop\src-tauri`

优先命令：

```powershell
cargo fmt --check
cargo test
cargo clippy -- -D warnings
```

## 下一步

1. 恢复/安装项目使用的 Rust 工具链，先跑上述三项 Rust gate。
2. 在 `f565fc4` 上执行桌面、Cloudflare、native WDIO 等非部署验证矩阵。
3. 继续迁移剩余 legacy process observer，所有七类 operation 的实际 sealed trace producer 接线完成后，再退休 active Tauri V1 reporter。
4. 明确产品是否要求重启 payload replay；若要求，实现 protected payload vault/run-record；否则保留并正式记录当前 durable-loss 语义。
5. 生成并校验新的 Git bundle。
6. 只有获得明确授权后，才执行 VMProtect Lite GUI、签名、NSIS、安装/卸载和真机 smoke；不得把源码/gate ready 表述为已经被 VMProtect 真实保护。

## 重要约束

- 禁止 `git reset --hard`、`git clean`、广泛 worktree prune、回滚已有有效改动。
- 不得删除 `.workbuddy\`。
- 不得删除或覆盖其他 worktree 的未提交源码：`sealed-spool-facade`、`producer-sentinel-static`、`process-trace-adapter`、`planc-producer-spool-adapter`、`refresh-spawn-race-fix`。
- 如清理临时文件，只能精确删除主工作区的 `C:\Users\17254\Desktop\存档\TOOL\VivoKsu 工具\refresh-spawn-race.patch`，并先确认其他 worktree 不依赖它。
